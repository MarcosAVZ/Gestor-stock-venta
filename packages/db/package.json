{
  "name": "@compras-whatsapp/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Prisma schema, generated client, and repository implementations. Provides @compras-whatsapp/db to apps/bot.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint \"src/**/*.ts\" \"tests/**/*.ts\" --max-warnings=0 --no-error-on-unmatched-pattern"
  },
  "dependencies": {
    "@compras-whatsapp/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
